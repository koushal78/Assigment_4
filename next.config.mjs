/** @type {import('next').NextConfig} */
const nextConfig = {
  
    images: {
        domains: ["media.better.com"],
        unoptimized: true,
        
        // Add the domain here
      },
};

export default nextConfig;
